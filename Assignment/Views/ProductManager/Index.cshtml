@model IEnumerable<Assignment.Models.ProductModel>

<p>
    <a asp-action="InputForm" >Create New</a>
    <div class="margin-right">
    <a asp-action="Searchform">Search</a>
    </div>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReleaseDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReleaseDate)
            </td>
            <td>
               @* @Html.ActionLink("Edit", "Edit", new {
                 Code=item.Code,
                    Name=item.Name,
                    Price=item.Price,
                    ReleaseDate=item.ReleaseDate
                }) |
               @Html.ActionLink("Details", "Details", new{ 
                    id=item.Id,
                    Code=item.Code,
                    Name=item.Name,
                    Price=item.Price,
                    ReleaseDate=item.ReleaseDate
                }) |
                
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
                <a href="/ProductManager/Edit/@item.Id">Edit</a> |
                <a href="/ProductManager/ShowDetails/@item.Id">Show Details</a> |

                <a id="shw" href="/ProductManager/Delete/@item.Id">Delete</a>
              
               

            </td>
        </tr>
}
    </tbody>
</table>
